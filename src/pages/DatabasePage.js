import React, { Component } from "react";
import { connect } from "react-redux";
import { open_menu } from "../store/actions/OpenMenu";
import { open_main_menu } from "../store/actions/MainMenu";
import { change_breadcrum } from "../store/actions/Bredcrum";
import DatabaseContainer from "../components/database/DatabaseContainer";

class DatabasePage extends Component {
  componentDidMount() {
    this.props.dispatch(open_menu("Database"));
    this.props.dispatch(open_main_menu("Database"));
    this.props.dispatch(change_breadcrum("Database"));
  }
  render() {
    return (
      <div>
        <div className="mb-4">
          <center>{/* <img src={data} className="img-fluid" /> */}</center>
        </div>
        <DatabaseContainer />
      </div>
    );
  }
}

const mapStoreToProps = (store) => {
  return {
    data: store,
  };
};

export default connect(mapStoreToProps)(DatabasePage);
